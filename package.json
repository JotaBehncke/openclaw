{
  "name": "openclaw-bot",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install",
    "start": "npx openclaw onboard --non-interactive"
  },
  "dependencies": {
    "openclaw": "latest"
  }
}
